# OPENSPEC:START
# OpenSpec shell completions configuration
fpath=("/Users/jgordijn/.oh-my-zsh/custom/completions" $fpath)
# OPENSPEC:END

# For profiling this should be at the top
# zmodload zsh/zprof
PROMPTER="starship"

if [[ "$TERM" == "xterm-ghostty" ]]; then
  export TERM=xterm-256color
fi

# Auto-start tmux only in Ghostty (not in Zed, VS Code, or other IDE terminals)
# if [[ -z "$TMUX" ]] && [[ "$TERM_PROGRAM" == "ghostty" ]] && command -v tmux &>/dev/null; then
#   exec tmux new-session -c "$HOME"
# fi

export PATH=$HOME/bin:$PATH:~/go/bin
export ZSH_CONFIGS=$HOME/zsh
export ZSH_DATA_DIR="${XDG_DATA_HOME:-$HOME/.local/share}/zsh"
[[ -d $ZSH_DATA_DIR ]] || mkdir -p $ZSH_DATA_DIR
export DOTFILES=$HOME/.config/zsh
export CUSTOM_ZSH_FUNCTIONS=$DOTFILES/scripts
export PLUGINS_DIR=$HOME/.config/zsh/plugins

# Feature flags for optional zsh scripts
export ZSH_ATUIN_ENABLED=true
# export ZSH_DOTENV_ENABLED=true

# Own plugin manager
source $DOTFILES/plugin_manager.zsh

export EDITOR=nvim
export VISUAL=nvim
export ZVM_LAZY_KEYBINDINGS=false
ZVM_INIT_MODE=sourcing


# Plugins:
zsh_add_plugin "Aloxaf/fzf-tab"
zsh_add_plugin "zsh-users/zsh-autosuggestions"
zsh_add_plugin "zsh-users/zsh-syntax-highlighting"
#zsh_add_plugin "MichaelAquilina/zsh-you-should-use"
zsh_add_plugin "jeffreytse/zsh-vi-mode"


# Via brew:
# - fzf
# - zoxide
# - bat
# - eza

if [ "$PROMPTER" = "powerlevel10k" ]; then
  # Enable Powerlevel10k instant prompt. Should stay close to the top of ~/.zshrc.
  # Initialization code that may require console input (password prompts, [y/n]
  # confirmations, etc.) must go above this block; everything else may go below.
  if [[ -r "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh" ]]; then
    source "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh"
  fi
fi

# 4. Initialize completion system once, after all completion paths are configured
source $DOTFILES/lib/completion_init.zsh
source $DOTFILES/lib/widget_init.zsh

zsh_add_default_homebrew_completion_path
zsh_run_compinit_once "${ZDOTDIR:-$HOME}/.zcompdump" "$HOME/.zcompcache"

compdef _code code-insiders

# Make sure completion cache is properly set up
zstyle ':completion:*' use-cache on
zstyle ':completion:*' cache-path "$HOME/.zcompcache"

# Improve completion system reliability
zstyle ':completion:*' completer _expand_alias _complete _ignored _approximate
# zstyle ':completion:*' completer _complete _approximate
zstyle ':completion:*' matcher-list 'm:{a-zA-Z-_}={A-Za-z_-}' 'r:|[._-]=* r:|=*' 'l:|=* r:|=*'

# Completion options (menu disabled for fzf-tab compatibility)
zstyle ':completion:*' list-colors "${(s.:.)LS_COLORS}"
zstyle ':completion:*' verbose yes
zstyle ':completion:*' group-name ''

zstyle ':completion:*:descriptions' format '%F{green}%B--- %d ---%b%f'

# This enables Zsh to understand commands like `docker run -it ubuntu`. However, by enabling this, this also
# makes Zsh complete `docker run -u<tab>` with `docker run -uapprox` which is not valid. The users have to put
# the space or the equal sign themselves before trying to complete.
zstyle ':completion:*:*:docker:*' option-stacking yes
zstyle ':completion:*:*:docker-*:*' option-stacking yes

# allow to watch with aliases
alias watch='watch '

alias chafa="chafa -f kitty -s 40x30 --passthrough=tmux"

alias rg='rg --hidden --smart-case --follow'

alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'

export MANPAGER="sh -c 'col -bx | bat -l man -p'"
export MANROFFOPT="-c"

# Auto cd into directories
setopt autocd
setopt AUTO_PUSHD           # Push the current directory visited on the stack.
setopt INTERACTIVE_COMMENTS # Allow comments in interactive shell (useful for copy-paste)

# Safe paste: prevents pasted text with newlines from executing immediately
# You must press Enter after pasting to execute
autoload -Uz bracketed-paste-magic
zsh_invoke_if_widget_shell zle -N bracketed-paste bracketed-paste-magic
setopt PUSHD_IGNORE_DUPS    # Do not store duplicates in the stack.
setopt PUSHD_SILENT         # Do not print the directory stack after pushd or popd.
alias ds='dirs -v'
for index ({1..9}) alias "$index"="cd +${index}"; unset index

# 3. Custom functions and key bindings
# Refresh config with F5
alias reload="exec zsh"
zsh_invoke_if_widget_shell bindkey -s "^[[15~" "reload^M"

# Source all completion scripts in ~/bin/completions
for f in ~/bin/completions/_*; do
  source "$f"
done

# Setup zoxide
eval "$(zoxide init zsh)"

# Create an improved zoxide interactive widget function
zi-widget() {
  # Save current buffer content
  local BUFFER_BACKUP=$BUFFER
  local CURSOR_BACKUP=$CURSOR

  # Clear the line to avoid confusion
  BUFFER=""
  zle redisplay

  # Run zoxide in interactive mode and get the result
  local result=$(zoxide query -i)

  # If a directory was selected, change to it
  if [ -n "$result" ]; then
    cd "$result"
    # Clear and redraw prompt via zle reset
    zle reset-prompt
  else
    # If canceled, restore original command line
    BUFFER=$BUFFER_BACKUP
    CURSOR=$CURSOR_BACKUP
    zle redisplay
  fi
}

zi() {
  local result=$(zoxide query -i "$@")
  if [ -n "$result" ]; then
    cd "$result" || return
  fi
}
zf() {
  local dir
  dir=$(zoxide query -l | fzf --reverse --height 40% --preview 'ls -la {}')
  if [ -n "$dir" ]; then
    cd "$dir" || return
  fi
}

# Create the widget
zsh_invoke_if_widget_shell zle -N zi-widget

# Source my setup
if [ -d $CUSTOM_ZSH_FUNCTIONS ]; then
    files=$(fd "^[0-9]+.*\.z?sh$" --no-ignore-vcs $CUSTOM_ZSH_FUNCTIONS | sort)
    for i in ${(f)files}; do # param extension flag 'f' strips newlines
        if [ -r $i ]; then
            source $i
        fi
    done
    unset i
fi

# Bind Ctrl+F to the widget
zsh_invoke_if_widget_shell bindkey "\ef" zi-widget
zsh_invoke_if_widget_shell bindkey -M viins "\ef" zi-widget



#THIS MUST BE AT THE END OF THE FILE FOR SDKMAN TO WORK!!!
export SDKMAN_DIR="$HOME/.sdkman"
[[ -s "$HOME/.sdkman/bin/sdkman-init.sh" ]] && source "$HOME/.sdkman/bin/sdkman-init.sh"



if [ "$PROMPTER" = "starship" ]; then
    # ~~~ Starship ~~~
    eval "$(starship init zsh)"
elif [ "$PROMPTER" = "oh-my-posh" ]; then
    # ~~~ Oh my posh ~~~
    eval "$(oh-my-posh init zsh)"
elif [ "$PROMPTER" = "powerlevel10k" ]; then
    # ~~~ powerlevel10k ~~~
    # p10k prompt (no longer maintained)
    source $ZSH_CONFIGS/plugins/powerlevel10k/powerlevel10k.zsh-theme
    # To customize prompt, run `p10k configure` or edit ~/.p10k.zsh.
    [[ ! -f ~/.p10k.zsh ]] || source ~/.p10k.zsh
fi


# For profiling. Put this at the end of the file
# zprof

# Added by LM Studio CLI (lms)
export PATH="$PATH:/Users/jgordijn/.lmstudio/bin"
# End of LM Studio CLI section

export N8N_API_KEY=$(cat ~/.secrets/n8n-api-key)

# opencode
export PATH=/Users/jgordijn/.opencode/bin:$PATH
export PATH="/opt/homebrew/opt/libpq/bin:$PATH"

# Added by Antigravity
export PATH="/Users/jgordijn/.antigravity/antigravity/bin:$PATH"

# Amp CLI
export PATH="/Users/jgordijn/.amp/bin:$PATH"





# bun completions
[ -s "/Users/jgordijn/.bun/_bun" ] && source "/Users/jgordijn/.bun/_bun"

setopt allexport
source ~/.secrets/hetzner.env
source ~/.secrets/tailscale.env
unsetopt allexport


export SSH_AUTH_SOCK="$HOME/Library/Group Containers/2BUA8C4S2C.com.1password/t/agent.sock"

# --- AI Sandbox Functions (managed by setup-sandbox.sh) ---
ai-lab() {
    local ws_root="/Users/jgordijn/projects/ahold"
    local config="/Users/jgordijn/projects/ahold/.devcontainer/standard/devcontainer.json"
    devcontainer up --workspace-folder "$ws_root" --config "$config" && \
    devcontainer exec --workspace-folder "$ws_root" --config "$config" \
        zsh -c "cd '$PWD' 2>/dev/null || cd '$ws_root'; exec zsh"
}

ai-ops() {
    local ws_root="/Users/jgordijn/projects/ahold"
    local config="/Users/jgordijn/projects/ahold/.devcontainer/privileged/devcontainer.json"
    devcontainer up --workspace-folder "$ws_root" --config "$config" && \
    devcontainer exec --workspace-folder "$ws_root" --config "$config" \
        zsh -c "cd '$PWD' 2>/dev/null || cd '$ws_root'; exec zsh"
}

ai-lab-rebuild() {
    local ws_root="/Users/jgordijn/projects/ahold"
    local config="/Users/jgordijn/projects/ahold/.devcontainer/standard/devcontainer.json"
    devcontainer up --remove-existing-container --workspace-folder "$ws_root" --config "$config" && \
    devcontainer exec --workspace-folder "$ws_root" --config "$config" \
        zsh -c "cd '$PWD' 2>/dev/null || cd '$ws_root'; exec zsh"
}

ai-ops-rebuild() {
    local ws_root="/Users/jgordijn/projects/ahold"
    local config="/Users/jgordijn/projects/ahold/.devcontainer/privileged/devcontainer.json"
    devcontainer up --remove-existing-container --workspace-folder "$ws_root" --config "$config" && \
    devcontainer exec --workspace-folder "$ws_root" --config "$config" \
        zsh -c "cd '$PWD' 2>/dev/null || cd '$ws_root'; exec zsh"
}
# --- End AI Sandbox Functions ---
#

export OPENCODE_PERMISSION='{"*":"allow"}'
